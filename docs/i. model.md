## 7. The correct mental model (this is the key intuition)

A system is:
a machine that continuously:
- defines a state
- transforms it via change
- compresses history into memory

Everything else sits on top of this loop.
