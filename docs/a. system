## Definition of a System

A system is not a model or a dataset.

A system is:
* a rule for what exists, what changes, and what persists

Everything else (features, signals, indicators) is downstream of that.
